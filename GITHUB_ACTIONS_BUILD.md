# 🚀 Автоматическая сборка через GitHub Actions

GitHub Actions автоматически соберет Windows .exe **на серверах GitHub** каждый раз когда ты делаешь push!

---

## ✅ Что уже настроено

Создан файл `.github/workflows/build-desktop.yml` который:

1. ✅ **Автоматически запускается** при push в ветки main, master или claude/*
2. ✅ **Собирает backend** с PyInstaller на Windows сервере GitHub
3. ✅ **Собирает frontend** с npm
4. ✅ **Упаковывает в Electron** и создает .exe установщик
5. ✅ **Сохраняет готовый .exe** как artifact (доступен для скачивания)

---

## 🎯 Как это работает

### Автоматическая сборка при push:

```bash
git add .
git commit -m "your changes"
git push
```

**Готово!** GitHub автоматически начнет сборку.

---

## 📥 Как скачать готовый .exe

### Вариант 1: Через веб-интерфейс

1. Зайди на GitHub: https://github.com/ТВОЙ_USERNAME/premises_rental
2. Перейди во вкладку **"Actions"**
3. Выбери последний успешный запуск (зеленая галочка ✅)
4. Прокрути вниз до секции **"Artifacts"**
5. Скачай **"Premises-Rental-Windows-Installer"**

### Вариант 2: Прямая ссылка

После успешной сборки artifact доступен по ссылке:
```
https://github.com/ТВОЙ_USERNAME/premises_rental/actions
```

---

## 🏷️ Создание релиза (опционально)

Чтобы создать официальный релиз с версией:

```bash
# Создай тег с версией
git tag v1.0.0

# Отправь тег на GitHub
git push origin v1.0.0
```

GitHub Actions автоматически создаст **Release** с готовым .exe файлом!

Релиз будет доступен по адресу:
```
https://github.com/ТВОЙ_USERNAME/premises_rental/releases
```

---

## 🔧 Ручной запуск сборки

Можешь запустить сборку вручную без push:

1. Зайди на GitHub → **Actions**
2. Выбери **"Build Desktop Application"** в левом меню
3. Нажми **"Run workflow"** → **"Run workflow"**
4. Жди ~10-15 минут
5. Скачай готовый .exe из Artifacts

---

## 📊 Процесс сборки

GitHub Actions выполняет следующие шаги:

```
1. Checkout code                   [~10 сек]
2. Setup Python 3.11               [~30 сек]
3. Setup Node.js 18                [~20 сек]
4. Install Python dependencies     [~2 мин]
5. Build Backend (PyInstaller)     [~3 мин]
6. Install Frontend dependencies   [~1 мин]
7. Build Frontend (Vite)           [~1 мин]
8. Prepare Electron files          [~10 сек]
9. Install Electron dependencies   [~1 мин]
10. Build Electron (NSIS)          [~3 мин]
11. Upload artifacts               [~1 мин]

ИТОГО: ~10-15 минут
```

---

## 📦 Что получится

После успешной сборки будут доступны **2 артефакта**:

### 1. **Premises-Rental-Windows-Installer** (рекомендуется)
- Файл: `Premises Rental System-Setup-1.0.0.exe`
- Размер: ~250-300 MB
- Тип: Установщик NSIS
- Создаст ярлыки, добавит в меню Пуск, настроит удаление

### 2. **Premises-Rental-Windows-Portable**
- Папка: `win-unpacked/`
- Размер: ~300-350 MB
- Тип: Portable версия
- Просто распаковать и запустить `Premises Rental System.exe`

---

## 🐛 Troubleshooting

### Сборка не запустилась

**Проблема:** Push не запускает Actions

**Решение:**
1. Проверь что Actions включены: Settings → Actions → "Allow all actions"
2. Проверь что push в правильную ветку (main, master, или claude/*)

### Сборка упала с ошибкой

**Где смотреть логи:**
1. GitHub → Actions → Выбери упавший запуск
2. Кликни на красный крестик ❌
3. Смотри логи каждого шага

**Типичные ошибки:**

- **"Backend exe not found"** → Проблема с PyInstaller
  - Проверь `backend/premises_rental.spec`
  - Проверь что все зависимости в `requirements.txt`

- **"Frontend build failed"** → Проблема с npm
  - Проверь `frontend/package.json`
  - Проверь синтаксис в React компонентах

- **"Electron build failed"** → Проблема с Electron Builder
  - Проверь `electron/package.json`
  - Проверь что все файлы скопированы

### Artifact не появился

**Решение:**
- Artifacts доступны **только 30 дней**
- Перезапусти workflow вручную (Actions → Run workflow)

---

## ⚡ Оптимизация скорости сборки

Сейчас сборка занимает ~10-15 минут. Можно ускорить:

### 1. Кэширование зависимостей (уже включено)
```yaml
cache: 'pip'      # Python зависимости
cache: 'npm'      # Node.js зависимости
```

### 2. Параллельная сборка (будущее улучшение)
- Собирать backend и frontend параллельно
- Можно сократить время до ~8 минут

---

## 💰 Лимиты GitHub Actions

**Free tier (бесплатный GitHub):**
- ✅ 2000 минут/месяц для публичных репозиториев
- ✅ 500 MB artifact storage
- ✅ Unlimited runs для публичных репо

**Расход:**
- 1 сборка = ~15 минут
- ~133 сборки в месяц (бесплатно!)

**Pro tip:** Делай сборку только при важных изменениях, используй ручной запуск.

---

## 🔐 Приватные репозитории

Если репозиторий приватный:
- Free: 500 минут/месяц (~33 сборки)
- Pro: 3000 минут/месяц (~200 сборок)

---

## 🎯 Следующие шаги

После того как Actions запущен:

1. **Отслеживай прогресс:** GitHub → Actions → Текущий запуск
2. **Дождись завершения:** ~10-15 минут (зеленая галочка ✅)
3. **Скачай .exe:** Artifacts → Premises-Rental-Windows-Installer
4. **Протестируй:** Запусти установщик на Windows
5. **Опубликуй релиз:** Создай тег `v1.0.0` если всё работает

---

## 📝 Примеры использования

### Разработка:
```bash
# Локальные изменения
git add .
git commit -m "fix: исправлена ошибка авторизации"
git push

# GitHub автоматически соберет новую версию
# Скачай из Artifacts и протестируй
```

### Релиз:
```bash
# Готовая версия для пользователей
git tag v1.0.0
git push origin v1.0.0

# GitHub создаст Release с .exe
# Отправь ссылку пользователям
```

---

## ✅ Готово!

Теперь **каждый push** будет автоматически собирать готовый .exe!

**Первый запуск:**
```bash
git add .github/workflows/build-desktop.yml
git commit -m "feat: добавлен GitHub Actions для автосборки"
git push
```

Иди на GitHub → Actions и наблюдай волшебство! 🎉

---

## 🆘 Нужна помощь?

1. Проверь логи в Actions
2. Найди ошибку в конкретном шаге
3. Исправь и сделай новый push
4. GitHub автоматически пересоберет

**Удачи!** 🚀
