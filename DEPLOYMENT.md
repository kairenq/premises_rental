# 🚀 Deployment Guide: Render + Netlify

Полное руководство по развертыванию Premises Rental System на Render (backend) и Netlify (frontend).

---

## 📋 Предварительные требования

1. **GitHub аккаунт** с вашим репозиторием
2. **Render аккаунт** - [render.com](https://render.com) (бесплатный)
3. **Netlify аккаунт** - [netlify.com](https://netlify.com) (бесплатный)

---

## 🔧 Часть 1: Деплой Backend на Render

### Шаг 1: Подготовка (уже выполнено)

Проект уже подготовлен со следующими файлами:
- ✅ `backend/Procfile` - команда запуска
- ✅ `render.yaml` - конфигурация Render
- ✅ `backend/.env.example` - пример переменных окружения
- ✅ Обновленный `config.py` с поддержкой переменных окружения

### Шаг 2: Создание Web Service на Render

1. **Перейдите на [Render Dashboard](https://dashboard.render.com/)**

2. **Нажмите "New +" → "Web Service"**

3. **Подключите ваш GitHub репозиторий:**
   - Выберите "Connect a repository"
   - Авторизуйте Render для доступа к GitHub (если нужно)
   - Выберите репозиторий `premises_rental`

4. **Настройте Web Service:**

   ```
   Name: premises-rental-api
   Region: Oregon (US West) или ближайший к вам
   Branch: main (или ваша текущая ветка)
   Root Directory: backend
   Runtime: Python 3

   Build Command:
   pip install -r requirements.txt

   Start Command:
   uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

5. **Выберите план:**
   - **Free** (достаточно для тестирования)
   - ⚠️ Free tier засыпает после 15 минут неактивности

### Шаг 3: Настройка переменных окружения

В разделе **Environment Variables** добавьте:

```bash
# Обязательные
SECRET_KEY=ваш-супер-секретный-ключ-минимум-32-символа-abc123def456
ENVIRONMENT=production
DATABASE_URL=sqlite:///./premises_rental.db

# Опционально (добавите позже после деплоя frontend)
FRONTEND_URL=https://ваш-сайт.netlify.app
```

**Как сгенерировать SECRET_KEY:**
```python
import secrets
print(secrets.token_urlsafe(32))
```

### Шаг 4: Настройка Persistent Disk (важно для SQLite!)

Без этого шага база данных будет сбрасываться при каждом перезапуске!

1. В настройках сервиса найдите **"Disks"**
2. Нажмите **"Add Disk"**
3. Настройки:
   ```
   Name: data
   Mount Path: /opt/render/project/src
   Size: 1 GB (достаточно для начала)
   ```
4. Сохраните

### Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Render начнет автоматический деплой
3. Дождитесь завершения (обычно 3-5 минут)
4. Ваш API будет доступен по адресу: `https://premises-rental-api.onrender.com`

### Шаг 6: Проверка Backend

Откройте в браузере:
```
https://ваш-сервис.onrender.com/
https://ваш-сервис.onrender.com/health
https://ваш-сервис.onrender.com/docs  (Swagger UI)
```

✅ **Backend готов!** Скопируйте URL, он понадобится для frontend.

---

## 🎨 Часть 2: Деплой Frontend на Netlify

### Шаг 1: Подготовка (уже выполнено)

Проект подготовлен со следующими файлами:
- ✅ `frontend/netlify.toml` - конфигурация Netlify
- ✅ `frontend/.env.example` - пример переменных окружения
- ✅ Обновленный `api.js` с поддержкой переменных окружения

### Шаг 2: Создание сайта на Netlify

1. **Перейдите на [Netlify Dashboard](https://app.netlify.com/)**

2. **Нажмите "Add new site" → "Import an existing project"**

3. **Выберите Git provider:** GitHub

4. **Выберите ваш репозиторий:** `premises_rental`

### Шаг 3: Настройка Build Settings

Netlify автоматически определит настройки из `netlify.toml`, но проверьте:

```
Base directory: frontend
Build command: npm run build
Publish directory: frontend/dist
```

### Шаг 4: Настройка переменных окружения

**ОЧЕНЬ ВАЖНО!** В разделе **"Site configuration" → "Environment variables"** добавьте:

```bash
VITE_API_URL=https://ваш-сервис.onrender.com
```

⚠️ **Замените** `ваш-сервис.onrender.com` на реальный URL вашего Render сервиса!

**Как добавить:**
1. Нажмите "Add a variable"
2. Key: `VITE_API_URL`
3. Value: `https://premises-rental-api.onrender.com` (ваш URL)
4. Выберите "All scopes"
5. Нажмите "Create variable"

### Шаг 5: Деплой

1. Нажмите **"Deploy site"**
2. Netlify начнет сборку (3-5 минут)
3. После завершения получите URL типа: `https://random-name-12345.netlify.app`

### Шаг 6: Настройка кастомного домена (опционально)

1. В настройках сайта → **"Domain management"**
2. Нажмите **"Add custom domain"**
3. Следуйте инструкциям Netlify

---

## 🔄 Часть 3: Связывание Backend и Frontend

### Обновление FRONTEND_URL на Render

Теперь, когда у вас есть Netlify URL:

1. Вернитесь в **Render Dashboard**
2. Откройте ваш Web Service
3. Перейдите в **"Environment"**
4. Добавьте/обновите:
   ```
   FRONTEND_URL=https://ваш-сайт.netlify.app
   ```
5. Сохраните (сервис автоматически перезапустится)

### Тестирование

1. Откройте ваш Netlify сайт
2. Попробуйте зарегистрироваться
3. Войдите в систему
4. Проверьте основной функционал

---

## 🎯 Часть 4: Создание первого администратора

Так как база данных пустая, нужно создать первого admin пользователя:

### Вариант 1: Через Render Shell

1. В Render Dashboard → ваш сервис → **"Shell"** (справа вверху)
2. Выполните:

```bash
cd backend
python3 << EOF
from app.db.database import SessionLocal
from app.models.models import User
from app.core.security import get_password_hash

db = SessionLocal()

# Создаем admin пользователя
admin = User(
    full_name="Admin User",
    email="admin@example.com",
    phone="+1234567890",
    role="admin",
    password_hash=get_password_hash("admin123")
)

db.add(admin)
db.commit()
print("Admin created successfully!")
db.close()
EOF
```

### Вариант 2: Через API + ручное изменение

1. Зарегистрируйтесь на сайте через UI
2. В Render Shell измените роль:

```bash
cd backend
python3 << EOF
from app.db.database import SessionLocal
from app.models.models import User

db = SessionLocal()
user = db.query(User).filter(User.email == "your@email.com").first()
if user:
    user.role = "admin"
    db.commit()
    print(f"User {user.email} is now admin!")
else:
    print("User not found")
db.close()
EOF
```

### Вход как администратор

Теперь войдите с учетными данными администратора и получите доступ к Admin Panel!

---

## 📝 Часть 5: Заполнение базы данных

После входа как админ, через Admin Panel добавьте:

1. **Companies** - хотя бы 1 компанию
2. **Buildings** - несколько зданий
3. **Categories** - категории помещений (офис, склад, торговое и т.д.)
4. **Rooms** - помещения для аренды
5. **Photos** - загрузите фотографии помещений

---

## 🔧 Часть 6: Автоматические деплои

### GitHub Integration

Оба сервиса уже настроены на автодеплой:

- **Render**: автоматически деплоит при push в main
- **Netlify**: автоматически деплоит при push в main

Просто делайте `git push` и изменения автоматически появятся в продакшене!

---

## 🐛 Troubleshooting

### Backend не запускается

1. **Проверьте логи** в Render Dashboard → Logs
2. **Проверьте переменные окружения**
3. **Проверьте Disk** - он должен быть подключен

### Frontend не может подключиться к Backend

1. **Проверьте VITE_API_URL** в Netlify environment variables
2. **Проверьте CORS** - в Render должен быть установлен FRONTEND_URL
3. **Откройте консоль браузера** (F12) и проверьте ошибки
4. **Проверьте Network tab** - какие запросы идут и с какими ошибками

### База данных сбрасывается

- ✅ **Убедитесь что Persistent Disk настроен** в Render
- Путь должен быть `/opt/render/project/src`

### Изображения не загружаются

1. Проверьте что `VITE_API_URL` установлен в Netlify
2. Убедитесь что загружаете фото через admin panel после деплоя
3. Проверьте что Disk настроен (фотографии хранятся там же)

### Free tier Render засыпает

- Бесплатный план Render засыпает после 15 минут неактивности
- Первый запрос после сна занимает 30-50 секунд
- Решение: апгрейд до платного плана ($7/мес) или use a uptime monitor

---

## 💰 Стоимость

### Бесплатный вариант:
- **Render Free**: $0/месяц
  - ⚠️ Засыпает после 15 мин
  - ⚠️ 750 часов/месяц (достаточно для одного сервиса)
- **Netlify Free**: $0/месяц
  - 100 GB bandwidth
  - 300 build minutes

### Продакшен вариант:
- **Render Starter**: $7/месяц
  - Всегда активен
  - 0.5 GB RAM
- **Netlify Pro**: $19/месяц (опционально)
  - Больше bandwidth
  - Analytics

---

## 🔐 Безопасность в продакшене

1. ✅ Измените SECRET_KEY на случайную строку
2. ✅ Используйте сильные пароли для admin
3. ✅ Регулярно делайте бэкапы базы данных (скачивайте через Render Shell)
4. ✅ Настройте мониторинг (Render alerts)
5. ✅ Используйте HTTPS (автоматически на Render и Netlify)

---

## 📊 Мониторинг

### Render
- Dashboard → ваш сервис → Metrics
- Логи в реальном времени
- Настройка алертов

### Netlify
- Site → Analytics (в платной версии)
- Deploy logs
- Function logs

---

## 🔄 Обновление приложения

```bash
# Внесите изменения в код
git add .
git commit -m "Update: описание изменений"
git push origin main

# Render и Netlify автоматически задеплоят изменения
```

---

## 🎉 Готово!

Ваше приложение теперь в production!

**Backend**: `https://ваш-сервис.onrender.com`
**Frontend**: `https://ваш-сайт.netlify.app`
**API Docs**: `https://ваш-сервис.onrender.com/docs`

---

## 📞 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Netlify Documentation](https://docs.netlify.com)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)
- [Vite Environment Variables](https://vitejs.dev/guide/env-and-mode.html)

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в Render и консоль браузера
2. Убедитесь что все environment variables установлены
3. Проверьте что Persistent Disk настроен
4. Создайте issue в репозитории с описанием проблемы

Удачи с деплоем! 🚀
