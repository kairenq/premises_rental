# Premises Rental System

Полнофункциональная информационная система для аренды помещений, разработанная на FastAPI (backend) и React (frontend) с использованием SQLite базы данных.

## 🚀 Возможности

### Для пользователей:
- 🔐 Регистрация и аутентификация
- 🏢 Просмотр доступных помещений с фильтрацией
- ❤️ Добавление помещений в избранное
- 📝 Создание договоров аренды
- ⭐ Оставление отзывов
- 🔧 Создание заявок на обслуживание
- 💳 Просмотр истории платежей

### Для администраторов:
- 👥 Управление пользователями
- 🏢 CRUD операции для компаний и зданий
- 🏠 Управление помещениями и категориями
- 📸 Загрузка фотографий помещений
- 📋 Управление договорами аренды
- 💰 Управление платежами
- 🔧 Обработка заявок на обслуживание

## 📋 Требования

- Python 3.8+
- Node.js 16+
- npm или yarn

## 🛠️ Установка и запуск

### Backend (FastAPI)

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
- Windows:
```bash
venv\Scripts\activate
```
- Linux/Mac:
```bash
source venv/bin/activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

5. Запустите сервер:
```bash
python run.py
```

Backend будет доступен по адресу: `http://localhost:8000`
API документация (Swagger): `http://localhost:8000/docs`

### Frontend (React)

1. Откройте новый терминал и перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите development сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:3000`

## 🗄️ Структура базы данных

Система использует SQLite с 14 таблицами:

### Основные таблицы:
- **users** - пользователи системы
- **companies** - компании-владельцы недвижимости
- **buildings** - здания
- **rooms** - помещения для аренды
- **room_categories** - категории помещений
- **room_photos** - фотографии помещений

### Операционные таблицы:
- **leases** - договора аренды
- **payments** - платежи
- **reviews** - отзывы пользователей
- **maintenance_requests** - заявки на обслуживание

### Вспомогательные таблицы:
- **favorites** - избранные помещения пользователей
- **activity_log** - журнал активности
- **notifications** - уведомления
- **lease_history** - история изменений договоров

## 👤 Первый вход

### Создание администратора

1. Зарегистрируйтесь через интерфейс: `http://localhost:3000/register`
2. Используйте роль "User" при регистрации
3. Вручную измените роль в базе данных на "admin":

```python
# Откройте Python shell в директории backend
python

# Выполните:
from app.db.database import SessionLocal
from app.models.models import User

db = SessionLocal()
user = db.query(User).filter(User.email == "your@email.com").first()
user.role = "admin"
db.commit()
```

Или используйте SQLite браузер для изменения поля `role` на `admin`.

### Тестовые данные

После входа как администратор, добавьте через админ панель:
1. Компанию (Companies)
2. Здание (Buildings)
3. Категории помещений (Categories)
4. Помещения (Rooms)
5. Фотографии к помещениям

## 🏗️ Структура проекта

```
premises_rental/
├── backend/
│   ├── app/
│   │   ├── api/           # API endpoints
│   │   ├── core/          # Конфигурация и безопасность
│   │   ├── db/            # Настройка БД
│   │   ├── models/        # SQLAlchemy модели
│   │   └── schemas/       # Pydantic схемы
│   ├── uploads/           # Загруженные файлы
│   ├── requirements.txt
│   └── run.py
│
├── frontend/
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── contexts/      # Context providers
│   │   ├── pages/         # Страницы приложения
│   │   ├── services/      # API сервисы
│   │   └── main.jsx       # Точка входа
│   ├── package.json
│   └── vite.config.js
│
└── README.md
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход
- `GET /api/v1/auth/me` - Текущий пользователь

### Помещения
- `GET /api/v1/rooms` - Список помещений
- `POST /api/v1/rooms` - Создание помещения (admin)
- `PUT /api/v1/rooms/{id}` - Обновление (admin)
- `DELETE /api/v1/rooms/{id}` - Удаление (admin)
- `POST /api/v1/rooms/{id}/photos` - Загрузка фото (admin)

### Договора аренды
- `GET /api/v1/leases` - Список договоров
- `POST /api/v1/leases` - Создание договора
- `GET /api/v1/leases/{id}/payments` - Платежи по договору

И многие другие... (см. `/docs`)

## 🎨 Технологии

### Backend:
- **FastAPI** - Современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Pydantic** - Валидация данных
- **JWT** - Аутентификация
- **SQLite** - База данных

### Frontend:
- **React 18** - UI библиотека
- **Vite** - Сборщик
- **Ant Design** - UI компоненты
- **Axios** - HTTP клиент
- **React Router** - Маршрутизация

## 📝 Лицензия

MIT

## 🤝 Контакты

При возникновении вопросов создайте issue в репозитории.