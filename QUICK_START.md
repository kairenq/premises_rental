# 🚀 Быстрый старт - Desktop приложение

## ⚠️ Если у тебя проблемы с pip - используй этот метод!

### Шаг 1: Проверь Python

Открой командную строку (cmd) и выполни:

```cmd
python --version
```

Должна показаться версия 3.10 или выше. Если нет - установи Python:
👉 https://www.python.org/downloads/

**ВАЖНО:** При установке поставь галочку "Add Python to PATH"!

---

### Шаг 2: Простая сборка

Запусти:

```cmd
build_desktop_venv.bat
```

Этот скрипт:
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Соберет backend в .exe
- ✅ Соберет frontend
- ✅ Подготовит Electron

---

### Шаг 3: Собери установщик

После того как предыдущий скрипт завершится:

```cmd
cd electron
npm run build
```

Готово! Установщик будет в `electron\dist\`

---

## 🐛 Проблемы и решения

### "Python не найден"

**Решение:**
1. Установи Python: https://www.python.org/downloads/
2. При установке включи "Add Python to PATH"
3. Перезапусти командную строку

### "npm не найден"

**Решение:**
1. Установи Node.js: https://nodejs.org/
2. Перезапусти командную строку

### "pyinstaller не найден"

**Решение:**
Используй `build_desktop_venv.bat` вместо `build_desktop.bat`

### "Backend build failed"

**Решение:**

1. Открой командную строку от администратора
2. Перейди в директорию backend:
   ```cmd
   cd backend
   ```
3. Создай виртуальное окружение:
   ```cmd
   python -m venv venv
   ```
4. Активируй его:
   ```cmd
   venv\Scripts\activate
   ```
5. Установи зависимости:
   ```cmd
   pip install pyinstaller
   pip install -r requirements.txt
   ```
6. Собери exe:
   ```cmd
   pyinstaller premises_rental.spec --clean
   ```

### "Access denied" / "Не удается найти указанный файл"

**Решение:**
- Запусти командную строку от администратора
- Отключи антивирус (временно)
- Проверь что путь не содержит русских букв

---

## 📦 Пошаговая ручная сборка

Если автоматические скрипты не работают:

### 1. Backend

```cmd
cd backend
python -m venv venv
venv\Scripts\activate
pip install pyinstaller
pip install -r requirements.txt
pyinstaller premises_rental.spec --clean --noconfirm
deactivate
cd ..
```

### 2. Frontend

```cmd
cd frontend
npm install
npm run build
cd ..
```

### 3. Копирование файлов

```cmd
cd electron
mkdir backend-exe
mkdir frontend-dist
copy ..\backend\dist\premises_rental_backend.exe backend-exe\
xcopy /E /I ..\frontend\dist\* frontend-dist\
```

### 4. Electron

```cmd
npm install
npm run build
```

Готово! Файл в `electron\dist\`

---

## 🎯 Тестирование без установщика

Запуск в режиме разработки:

```cmd
cd electron
npm start
```

Откроется окно приложения без сборки установщика.

---

## ✅ Минимальные требования

Для сборки нужны:
- Windows 10+
- Python 3.10+
- Node.js 18+
- 2 GB свободного места на диске

Для запуска готового .exe:
- Windows 10+
- 500 MB свободного места
- **Ничего больше не нужно!**

---

## 📞 Нужна помощь?

1. Проверь что все команды выполняются без ошибок
2. Смотри логи в консоли
3. Проверь DESKTOP_BUILD.md для подробностей

**Удачи!** 🚀
